============================================================
  SESSION 25 — 2026-04-15 evening + 2026-04-16 show day
  Culpeper Day 1 debrief + Day 2 live fixes
============================================================

WRITTEN FOR THE NEXT CLAUDE INSTANCE
------------------------------------
Massive session spanning two days. Culpeper launch, prior Claude
crashed at ~15:14 on Day 1. This session rebuilt context, shipped
a pile of fixes during Day 2 live scoring, and surfaced several
open issues to verify on the scoring PC.


VERSIONS DEPLOYED (as of end of session)
----------------------------------------
  Watcher:  v1.6.0 constant in code — MULTIPLE patches added on top
            without bumping past 1.6.0 (status scan, numeric fallback,
            heartbeat clock, idle timer removal). NEEDS a version bump
            to v1.7.0 before next formal release to scoring PCs.
  Funnel:   v1.3.0 (holdTarget feature — config not yet enabled on scoring PC)
  Worker:   deployed to Cloudflare via wrangler deploy ✓ (1-hour sweep live)
  Frontend: deployed to preview.westscoring.pages.dev ✓


SHIPPED THIS SESSION
--------------------
Watcher:
  - v1.6.0 peek overhaul: dedicated west_peek_log.txt, classifier
    verified against real HTML (PREVIOUS EXHIBITOR added), transition
    trigger fixed (any * → UPLOADED), self-healing dormancy (5-min
    cooldown instead of session-kill)
  - Heartbeat now sends WATCHER_VERSION (was hardcoded '2.2')
  - Heartbeat adaptive: 10s with clock snapshot when class active,
    60s when idle. Clock snapshot = classNum, entry, elapsed, ta,
    phase, jumpFaults, rank.
  - 30-min idle timer REMOVED (was prematurely closing classes)
  - Farmtek status: scan cols[36]-[39] for text status instead of
    fixed col[38] (Ryegate shifts the column between entries)
  - Farmtek numeric status FALLBACK: when text scan finds nothing,
    check col[21]/col[28]/col[35] for values 1-6 and map to status
    codes. This catches JO WDs (col[28]=4) that Farmtek writes
    numerically but never as text.
  - Round label on live clock card now uses oc.label (from watcher
    UDP parser) instead of computed.currentRound (often null with
    concurrent classes)

Funnel:
  - v1.3.0: holdTarget config option. When holdTarget=1, persists
    the Target field {18} across on-course frames so the scoreboard
    keeps showing the time-to-beat during the ride. Resets on rank
    {8}. RSServer output only — watcher gets raw frames.

Worker:
  - autoCompleteStaleClasses bumped from 15 min → 1 hour
  - Heartbeat stores clock snapshot in KV
  - /getLiveClass exposes heartbeatClock + watcherVersion

Frontend:
  - Stats R2/JO: mirrored R1 elim/WD treatment (status badge in
    place col, em-dash gap, dimmed row) into renderStandingsTable
  - results.html: entries with status codes but no time now render
    (was skipping RT/WD/EL entries that had no r1TotalTime)
  - live.html: reactivated classes purged from recent-results section
  - live.html: staleness badge (bottom-right of clock box) shows
    data age — blank < 5s, "Ns ago" < 30s, orange > 30s, red > 90s
  - live.html: concurrent-class fix (hide class tail when active > 1)
  - Heartbeat clock fallback in display-config.js — browser uses
    heartbeat's clock snapshot when onCourse data is stale

Linter:
  - scripts/lint-show.js shipped — 8 starter rules including
    partial-ingest detection (caught class 291 on first run)

Deploy infrastructure:
  - CURRENT-VERSION marker files in deploy folders
  - README cover versions bumped
  - Business OneDrive = the real deploy target (not the repo's
    personal OneDrive path)
  - Admin page shows watcher version on status line

Data patches (manual D1 fixes for Culpeper Day 1):
  - Class 291: RESOLVED. Orphaned row deleted via wrangler d1,
    Bill re-uploaded, entry 6228 (ROSALINDE SPF) never competed
    (hasGone=false, all-zero scores). Marked complete. The initial
    partial-ingest was caused by the Day 1 Claude crash at 15:14 —
    cls_raw stored but entries INSERT never completed.
  - Class 264: 4 WD entries patched, FINAL FOCUS Z OC→EL,
    FAHRENHEIT ADDICT phantom R1 data cleared + set RT
  - Class 212: NIKOLAAS RFB R2 WD added, SHUTTERFLY'S RT,
    ELLEN S EL, KT ORIGI EL, place fixes (MISTER GREY 7th,
    WDs tied 8th)
  - Classes 213, 236, 245: manually closed (peek missed them —
    were never Ctrl+A'd so peek never polled)


NUMERIC STATUS CODE TABLE (Farmtek .cls)
----------------------------------------
See display-config.js WEST.numericStatusMap for authoritative table.

  Num  Mapped  Status
  ---  ------  ------
  1    EL      tentative — not yet observed
  2    RT      confirmed: class 212 #6318, Ryegate shows RT
  3    OC      confirmed: class 264 #6056 — generic "eliminated" flag,
               text field gives specific reason when present
  4    WD      confirmed: multiple entries — Farmtek NEVER writes WD
               as text, numeric only
  5    RF      tentative — RF = Rider Fall
  6    DNS     tentative — not yet observed
  >6   --      NOT a status — scoring data (e.g. JO faults)

Round columns: R1=col[21], R2/JO=col[28], R3=col[35]
Text status: scan cols[36]-[39] for known codes (shifts between entries)


ENTRIES TO VERIFY ON SCORING PC (Ryegate)
-----------------------------------------
Bill needs to check these entries in Ryegate on the scoring PC
and report what status Ryegate shows. This confirms the numeric
mapping and whether our "generic EL" interpretation is correct.

  Numeric 3 with text EL:
    Class 212 #6147 ELLEN S - INB
    Class 212 #6192 KT ORIGI
    Class 294 #6356 LA FE PAYANA

  Numeric 3 with text RF:
    Class 220 #6116 OLYMPIA B
    Class 245 #6402 CABOT FTR

  Numeric 2 with no text (mapped RT):
    Class 212 #6318 SHUTTERFLY'S SHAMELESS


OPEN ITEMS FOR NEXT SESSION
----------------------------
  HIGH PRIORITY:
  - Farmtek two-phase transition detection — clock kept counting
    through phase change on Day 2. Need UDP log from a two-phase
    horse to diagnose. Key question: does Farmtek send a RANK signal
    between phases? Or just a TA change after a pause? The Target
    field {18} shows during countdown but disappears during on-course.
    Check west_udp_log.txt on scoring PC around a two-phase horse.
    TIMY sends explicit RANK signal; Farmtek may not.

  - Peek doesn't watch non-selected concurrent classes — classes
    213, 236, 245 all missed on Day 2 because peek only polls
    selectedClassNum. Bill's proposed fix:
      • When a class has been active > N minutes (30? 60?) without
        a CLASS_COMPLETE, do a one-shot peek of ryegate.live for
        that class.
      • If ryegate.live shows UPLOADED → fire CLASS_COMPLETE.
      • Could run on a timer that checks ALL active classes, not
        just the selected one.
      • Implementation: add a periodic sweep (every 5 min?) that
        iterates activeClasses, peeks any that aren't the currently-
        selected class, and closes any that ryegate shows as done.
      • This is separate from the main peek polling loop (which
        stays tied to selectedClassNum for real-time detection).

  - Classes 216 + 220 still need Day 1 data patches (same WD/status
    issues as 212/264). Pull Ryegate HTML, cross-reference, patch D1.

  - Verify numeric status codes on scoring PC (entry list above).
    Bill checks Ryegate at end of Day 2 and reports what status
    Ryegate shows for each flagged entry.

  - holdTarget=1 needs to be set in c:\west-funnel\config.json on
    the scoring PC to enable Target field persistence on scoreboard.
    Bill does this at next break or end of day.

  - Watcher version bump needed — code says v1.6.0 but has multiple
    unversioned patches on top (status scan, numeric fallback,
    heartbeat clock, idle timer removal). Bump to v1.7.0 with
    changelog before next formal scoring PC deployment.

  MEDIUM PRIORITY:
  - Phase 2 observability: audit_events + anomalies view (~3.5 hrs).
    Full spec below in PHASE 2 OBSERVABILITY SPEC section.
  - Linter rule expansion as new bug patterns surface
  - Smart scoreboard display (future exploration — separate process
    with custom phase layouts, animations, graphics)

  LOW PRIORITY / DEFERRED:
  - Watcher auto-download from website (host versions for scoring
    PCs to self-update)
  - Visual regression testing
  - ORDER_POSTED HTML sample from ryegate.live (grab at next show
    when a class has OOG posted but hasn't started)


PHASE 2 OBSERVABILITY SPEC (agreed 2026-04-15)
-----------------------------------------------
Goal: answer "was anything missed?" and "is the HTML the prob or
the database?" without sitting and watching the website.

Phase 1 (SHIPPED): scripts/lint-show.js — data-shape linter.
  8 rules, catches partial ingests, missing class types, ghost
  entries. Run: node scripts/lint-show.js --slug=<slug>

Phase 2 (NOT YET BUILT — ~3.5 hours):

  D1 SCHEMA — new table audit_events:
    id          INTEGER PRIMARY KEY AUTOINCREMENT
    show_id     INTEGER
    slug        TEXT
    ring        TEXT
    class_num   TEXT
    event_type  TEXT NOT NULL
    event_source TEXT    -- 'watcher' | 'worker' | 'admin' | 'cron'
    details_json TEXT
    created_at  TEXT NOT NULL

    CREATE INDEX idx_audit_slug_time ON audit_events(slug, created_at DESC);
    CREATE INDEX idx_audit_class ON audit_events(slug, ring, class_num, created_at DESC);

  EVENT TYPES to log (one row each):
    class_created         — first cls upload for a class
    class_selected        — CLASS_SELECTED UDP via watcher
    results_posted        — postClassData (entry count, delta)
    class_complete_fired  — markClassComplete (which signal: peek/Ctrl+A/.tod/admin)
    class_auto_completed  — autoCompleteStaleClasses (staleness in minutes)
    class_reopened        — reopenClassIfComplete
    show_state_change     — activateShow / autoCompleteShow
    admin_action          — /admin/* endpoints (action + args)
    error                 — catch blocks in major endpoints

  WORKER HELPER:
    async function logAudit(env, { slug, ring, class_num,
      event_type, event_source, details }) { ... }
    ~15 lines. Fails silently. Called from ~10 instrument points.

  ENDPOINTS:
    GET /admin/activity?slug=X&limit=200
      Raw timeline, newest first. Optional &ring=N&class_num=N.

    GET /admin/anomalies?slug=X
      Red-flag queries:
      - Dangling active classes (no CLASS_COMPLETE, not auto-completed)
      - class_complete fired but entry_count < cls_raw entry count
        (partial ingest — the class 291 fingerprint)
      - class_auto_completed rows (all primary signals missed)
      - Watcher heartbeat gaps > 5 min during show hours
      - Peek dormancy covering a class that later completed
      - Excessive results re-posts (>3x in 10 min on same class)

  UI PAGE:
    activity.html — Timeline tab + Anomalies tab.
    Linked from admin.html as "Activity" button.

  RETENTION:
    Prune events older than 90 days on insert.

  MIGRATION:
    CREATE TABLE + indexes via /admin/migrate (already exists).


STYLE / CONTEXT REMINDERS (STILL BINDING)
------------------------------------------
  - DISCUSS before coding. Wait for "ok do it."
  - No unrequested features. Keep sessions light when Bill asks.
  - Time: whole seconds with decimals. Non-FEI 3, FEI 2.
  - No popups. Multi-page layout only.
  - "OneDrive" = business OneDrive deploy target
    (C:\Users\bwort\OneDrive - Worthington Event Solutions &
    Technology LLC\Live Scoring Deployment\), NOT the personal
    OneDrive the repo lives on.
  - Watcher version bump + changelog + CURRENT-VERSION marker +
    sync to business OneDrive on every batch.
  - Claude instance name: "Skippy the Magnificent."
