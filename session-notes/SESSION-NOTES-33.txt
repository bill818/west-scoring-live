SESSION-NOTES-33 — jumper scoring schema pivot: wide → per-round
Date: 2026-04-24 (morning)
Duration: short focused session
Outcome: Session 32 shipped jumper scoring with a wide single-table
         design. Session 33 pivoted it to per-round (summary + rounds
         tables) after reviewing v2 stats.html + STATS-BRAINSTORM.md
         which confirmed per-round is the right shape for analytics.
         Transparent to admin — same wide row shape served via SQL
         pivot. 71 classes, 959 entries, 1,127 round rows backfilled.

═══════════════════════════════════════════════════════════════════════════
HEADLINE
═══════════════════════════════════════════════════════════════════════════

Bill pushed back on the wide-table design shipped in Session 32:
"if we waited until Phase 3 [stats] we may have a bigger task on our
hands." He asked to look at what stats existed in v2 before deciding.

Evidence review changed the call:

1. V2's D1 `results` table IS per-round (keyed by entry_id + round).
   V2 made the schema decision we were about to defer.

2. v2 stats.html client-side UNPIVOTS wide jumper data into per-round
   rows at lines 1262-1269 with the explicit comment:
   "Stats page expects D1-shaped entries with round sub-objects."
   The unpivot is a pain point — v2 does it every page render.

3. docs/STATS-BRAINSTORM.md (Session 12 brainstorm for v1/early-v2)
   — EVERY primary stats bullet is per-round:
     - Clear round % R1 / JO
     - Avg elapsed vs TA (per round)
     - Fastest clear round (per round)
     - JO conversion rate
     - Avg JO time vs R1 time
     - JO clear % vs R1 clear %
     - Fault patterns R1 vs JO
     - Time savings in JO
   Not a theoretical future — the core product value prop.

The "bigger task later" concern was legitimate because the per-round
consumer count isn't just "future stats" — it's rider pages, horse
pages, season leaderboards, TA analysis, public stats page, and more.
Writing all of those against wide and then flipping to per-round
would be expensive. Switching now while only one writer (parser) and
one reader (admin) depend on it is cheap.

═══════════════════════════════════════════════════════════════════════════
WHAT SHIPPED
═══════════════════════════════════════════════════════════════════════════

MIGRATION 009 — entry_jumper_per_round.sql
  - DROP TABLE entry_jumper_scores (Session 32's wide design)
  - CREATE TABLE entry_jumper_summary (entry_id PK, entry-scoped:
    ride_order, overall_place, score_parse_status, score_parse_notes,
    timestamps)
  - CREATE TABLE entry_jumper_rounds (entry_id+round composite PK,
    per-round: time, penalty_sec, total_time, time_faults,
    jump_faults, total_faults, status, numeric_status, timestamps)
  - Indexes on (entry_id) and (round) for query performance

WORKER write path (west-worker.js /v3/postCls)
  - Parser still emits wide shape per entry (unchanged).
  - Write-time split:
      1. UPSERT entry_jumper_summary with entry-scoped fields
      2. DELETE entry_jumper_rounds for this entry (fresh replace
         handles the case where a round previously had data and
         no longer does — operator edited in Ryegate)
      3. For each round 1..3: INSERT entry_jumper_rounds row ONLY
         if that round has data (non-zero time, OR any status).
         Absence of a round row = round didn't happen.
  - Zero-data rounds are skipped at write. entry_jumper_rounds
    only carries rows for rounds that actually happened.

WORKER read path (west-worker.js /v3/listEntries)
  - Pivots per-round rows back to wide shape via 3 LEFT JOINs:
      LEFT JOIN entry_jumper_summary s   ON s.entry_id = e.id
      LEFT JOIN entry_jumper_rounds r1   ON r1.entry_id = e.id AND r1.round = 1
      LEFT JOIN entry_jumper_rounds r2   ON r2.entry_id = e.id AND r2.round = 2
      LEFT JOIN entry_jumper_rounds r3   ON r3.entry_id = e.id AND r3.round = 3
  - Projects r1.time AS r1_time, r2.time AS r2_time, etc.
  - Missing rounds come back as NULL — admin renders blank cell via
    existing Decision 1 logic.
  - Admin code UNCHANGED. Same flat JSON shape as pre-pivot.

ADMIN (v3/pages/admin.html)
  - No changes. renderEntries / renderJumperRoundCell /
    renderJumperPlace all consume the same wide shape from
    /v3/listEntries that the pivot produces.

═══════════════════════════════════════════════════════════════════════════
VALIDATION
═══════════════════════════════════════════════════════════════════════════

End-to-end re-POSTed 71 .cls files through the new write path, then
verified listEntries for class 23 matches yesterday's Ryegate PDF
shape exactly:

  7 acceptance cases from SESSION-32 findings §10 all pass:
    146 ANYTIME     → placed 1,  R1 65.138 clean, R2 32.757 clean
    176 REMINISCENT → placed 10, R1 62.126 clean, R2 29/4 EL  ← per-round works
    184 DON REGAL   → placed 27, R1 31 RF
    107 GOLDEN GUN  → placed 27, R1 71/8 EL
    239 STAKKATO    → placed 27, R1 56/4 EL
    129 FARFAN M    → placed 26, R1 44/4 RT
    150 ZEALOUS     → DNS — no summary or round rows visible as null

Row counts reconcile with Session 32:
  entry_jumper_summary:   959 (one per jumper entry — matches)
  entry_jumper_rounds:  1,127  (874 R1 + 253 R2 + 0 R3)
    - 959 − 874 = 85 entries that didn't ride R1 (DNS)
    - 253 R2 rows = entries that rode R2/JO
    - 71 rows with text status — matches Session 32's 54+17 = 71
  parse warnings:  0

═══════════════════════════════════════════════════════════════════════════
ARCHITECTURAL DECISIONS LOCKED THIS SESSION
═══════════════════════════════════════════════════════════════════════════

9. PER-ROUND IS THE AUTHORITATIVE SHAPE FOR SCORING STORAGE.
   (Adds to SESSION-32 decisions 1-8.)
   entry_jumper_rounds is the source of truth for per-round data.
   entry_jumper_summary is the source of truth for entry-scoped data
   (place, ride_order, parse meta). Wide shape is a READ projection
   served by /v3/listEntries for UI consumers that want flat rows.

10. ABSENCE OF ROUND ROW = ROUND DIDN'T HAPPEN.
    No round rows with all-zero data. Query semantics: "did entry X
    ride R2?" becomes "exists entry_jumper_rounds WHERE entry_id=X
    AND round=2". No column-pivoting or NULL gymnastics.

11. FRESH-REPLACE ROUND ROWS ON EACH POST.
    When the engine re-POSTs a .cls, worker DELETEs all
    entry_jumper_rounds for each entry then INSERTs only the rounds
    that currently have data. Handles the case where Ryegate edit
    removes a round's data (rare but possible).

12. HUNTER SCHEMA WILL MIRROR THIS.
    When Phase 2d hunter-half lands:
      - entry_hunter_summary (go_order, current_place, parse meta)
      - entry_hunter_rounds (per round: total, numeric_status, status)
    Same split pattern. Future per-judge scores go in a third table
    (entry_hunter_judge_scores keyed by entry_id+round+judge) when
    needed — natural extension.

═══════════════════════════════════════════════════════════════════════════
RUNNING STATE AT END OF SESSION
═══════════════════════════════════════════════════════════════════════════

Cloudflare:
  - Worker version: 36523ef9-f669-4758-b425-6f0dfbc6d275
  - WEST_DB_V3 tables:
      shows, rings, classes, entries (unchanged)
      entry_jumper_summary (new)
      entry_jumper_rounds (new)
      entry_jumper_scores (DROPPED)
  - Migrations 008 (wide) now effectively superseded by 009
    (per-round). Both files preserved in git history.

Data:
  - 959 entry_jumper_summary rows
  - 1,127 entry_jumper_rounds rows across 51 T classes
  - 0 parse warnings

Admin: Still off preview per Bill. Local file:// test against the
new pivot confirms class 23 renders identically to yesterday.

═══════════════════════════════════════════════════════════════════════════
PART 2 — HUNTER HALF SHIPPED (same session, later morning)
═══════════════════════════════════════════════════════════════════════════

Applied the exact same per-round pattern to the hunter lens. Full
Phase 2d half-A (hunter data collection) landed end-to-end. Display
polish (HUNTER_MODES labels, Derby component breakdown, per-judge
scores table) still deferred per Bill's directive "collect the data
then we'll work on per-judge calculations, label displays etc."

MIGRATION 010 — entry_hunter_per_round.sql
  - entry_hunter_summary (entry_id PK: go_order, current_place,
    combined_total, score_parse_status, score_parse_notes, timestamps)
  - entry_hunter_rounds (entry_id+round composite PK: total, status,
    numeric_status, timestamps). Absence of row = didn't ride.
  - classes.num_judges + classes.is_equitation (from header col[7]
    and col[10])

LENS MODULE — v3/js/west-cls-hunter.js
  - Versioned shape (v_2026_04_24). Identity cols 0-12 shared w/ jumper.
  - go_order col[13], current_place col[14], combined_total col[45]
  - Per-round: total cols[42/43/44], numeric status cols[46/47/48],
    text status cols[52/53/54]
  - Per-judge positions documented (judges.N.start) but NOT consumed
    in Phase 2d — future entry_hunter_judge_scores table
  - Derby component cols (15-29 for classMode=2) noted as deferred

WORKER
  - parseClsHeaderV3 extended for H: captures num_judges + is_equitation
  - CLS_HUNTER_LAYOUT inline mirror of lens module v_2026_04_24
  - HUNTER_NUMERIC_MAP {2:'EL', 3:'RT'} (from v2 display-config.js:1390
    — narrower than jumper's map per v2 precedent)
  - parseEntriesScoreH — separate function (Article 1 at function level).
    Per-round text status direct read (no tail-scan ambiguity like
    Farmtek), numeric fallback via HUNTER_NUMERIC_MAP
  - /v3/postCls gains H branch: UPSERT entry_hunter_summary +
    fresh-replace entry_hunter_rounds (same pattern as jumper)
  - classes INSERT/UPSERT now binds num_judges + is_equitation
  - /v3/listEntries extended with hunter JOINs: entry_hunter_summary
    (aliased hs) + entry_hunter_rounds × 3 (hr1/hr2/hr3). Hunter
    fields aliased as r1_score_total, r1_h_status etc. to avoid
    collision with jumper r1_time/r1_status
  - ORDER BY uses COALESCE(jumper_place, hunter_place) so hunter
    entries sort naturally too

SHARED MODULES
  - v3/js/west-rules.js gains hunterIsPlaced + hunterPlaceFor.
    Rule: R1 is the GATE — must have valid total and non-killing
    status. Past R1 = placed. Later-round failures local (no ladder).
    Universal across hunter modes (no method table needed — matches
    Bill's SESSION-32 direction).

ADMIN
  - renderEntries gains isHunter branch with columns: Go | Pl | R1 | R2 | R3
  - renderHunterRoundCell: total value or status label (Decision 1
    suppression via WEST.status.isKillingStatus)
  - renderHunterPlace: delegates to WEST.rules.hunterPlaceFor

VALIDATION
  - 71 .cls files re-POSTed through new hunter parser path
  - 221 hunter summary rows across 16 H classes (100% coverage)
  - 58 hunter round rows (38 R1 + 16 R2 + 4 R3)
    ★ First R3 hunter data ever captured in v3 ★
  - 5 rows with text status present
  - 14 warnings — all expected "classMode=2 Derby components deferred"
    from 2 derby test classes (1000: 5 entries, 1001: 9 entries).
    Zero surprise warnings.
  - num_judges populated on all 16 H classes
  - Class 1002 spot-check (multi-judge classMode=0 O/F): entry 117
    RULE ON returned correctly with go=0, pl=1, combined=56, r1=28,
    r2=28 — matches the raw col[42]=28, col[43]=28, col[45]=56.

RUNNING STATE AFTER HUNTER HALF
  - Worker version: 80d31f0c-013c-46b0-9b1c-46abf3a5de18
  - WEST_DB_V3 total:
      entry_jumper_summary:   959 rows
      entry_jumper_rounds:  1,127 rows
      entry_hunter_summary:   221 rows
      entry_hunter_rounds:     58 rows
      classes:                 71 (51 J/T, 16 H, 4 U)
  - 0 parse warnings outside expected Derby notes
  - Admin renders both jumper AND hunter classes correctly (local,
    off preview still)

═══════════════════════════════════════════════════════════════════════════
PART 3 — FORCED HUNTER FIX (same session)
═══════════════════════════════════════════════════════════════════════════

Bill spotted that "forced 1-round hunters" (classes that just have
placements pinned by the operator, no computed scores) weren't
handled. He asked to verify via snapshots and notes.

Dug up the spec:
  H[02] class_mode     0=Over Fences, 1=Flat, 2=Derby, 3=Special
  H[05] scoring_type   0=Forced, 1=Scored, 2=Hi-Lo  ← MISSING PIECE
  H[07] num_judges

Forced = operator manually pins placements. Col[14] current_place
populated; col[42-45] totals stay 0. The hunterIsPlaced rule shipped
this morning would treat every Forced entry as unplaced (R1 total = 0
failed the gate).

Also confirmed (SESSION-NOTES-16): hunters don't write .cls on on-
course. UDP {fr}=11 INTRO is the only on-course signal. {fr}=14 fires
as each ribbon is announced for forced/flat. Those flow through the
live path (UDP), not the .cls path — unchanged by this fix.

MIGRATION 011 — classes.scoring_type INTEGER
  Single column added. parseClsHeaderV3 H-branch now reads col[5].

WORKER
  - classes UPSERT binds scoring_type alongside class_mode, num_judges,
    is_equitation
  - Header parser returns scoring_type on hunter classes

west-rules.js
  - hunterIsPlaced now takes (scoringType, r1Total, r1Status):
      scoringType === 0 (Forced): return !isKilling(r1Status)
        -- placement evidence is current_place presence; R1 total
           intentionally ignored (no scores to check)
      else: current rule (R1 gate with valid total + non-killing)
  - hunterPlaceFor signature updated to match

ADMIN
  - renderHunterPlace takes scoringType
  - renderEntries threads scoring_type from class object through

VALIDATION
  - 71 .cls files re-POSTed
  - 16 hunter classes now classified:
      classMode=0 Over Fences, scoring_type=0 Forced:   4 classes
      classMode=0 Over Fences, scoring_type=1 Scored:   9 classes
      classMode=2 Derby, scoring_type=1:                 2 classes
      classMode=3 Special, scoring_type=1:               1 class
  - Class 901 "3'3" Green Hunter" (Forced): 6 entries, all placed
    1-6, r1_score_total=null, combined_total=0 — renders correctly
    under new rule
  - Class 902 "Forced Eq Class": 4 entries, all placed 1-4 — ditto

Derby question — already covered earlier in session: totals at
col[42-45] captured, components (HighOptions/Handy bonus) NOT.
But Bill then revised: capture components AND per-judge data now,
display logic later. THAT work is the next task after this fix
commit.

RUNNING STATE AFTER FORCED FIX
  - Worker version: fded56f8-1152-4353-9337-e6e51ea3fc2a
  - classes.scoring_type populated on all 71 classes
  - Forced classes (4) now render placed entries correctly in admin

═══════════════════════════════════════════════════════════════════════════
PART 4 — HUNTER COMPLETION: PER-JUDGE + HEADER EXPANSION (evening)
═══════════════════════════════════════════════════════════════════════════

Closed the "collect everything knowable for hunters" scope. All
capture plumbing now in place; display polish still deferred.

MIGRATION 012 — hunter_judge_scores_and_header.sql
  NEW: entry_hunter_judge_scores (entry_id + round + judge_idx PK,
       base_score REAL, high_options REAL nullable, handy_bonus REAL
       nullable, timestamps). Single table handles both non-derby
       sequential and derby interleaved layouts via nullable cols.
  ALTER: classes gets num_rounds, score_method, ribbon_count,
         is_championship, sponsor, ihsa, derby_type — 7 new fields
         from hunter header positions.

PARSER — parseClsHeaderV3 H-branch
  Now reads 11 hunter header fields (previously 4):
    col[2]  class_mode       ← existing
    col[3]  num_rounds       ← NEW (H[03])
    col[5]  scoring_type     ← from earlier today
    col[6]  score_method     ← NEW (H[06] Total/Average)
    col[7]  num_judges       ← existing
    col[8]  ribbon_count     ← NEW (H[08])
    col[10] is_equitation    ← existing
    col[11] is_championship  ← NEW (H[11])
    col[29] sponsor          ← NEW (H[29] text)
    col[37] derby_type       ← NEW (H[37] 0-8)
    col[38] ihsa             ← NEW (H[38])

PARSER — parseHunterJudgeScores (new function)
  Reads per-judge scores per round. Layout switches on class_mode:
    Non-derby (0/1/3): sequential at col[15+j]/[24+j]/[33+j]
    Derby (2): interleaved — R1 stride=2 per judge [HighOpt, Base],
               R2 stride=3 [HighOpt, Base, Handy]
    Forced (scoring_type=0): skipped — no scores exist
  Skip rule: base_score null or 0 = judge didn't score that round
  (hunter scores range 40-100; 0 is Ryegate's empty-slot sentinel).

WORKER /v3/postCls
  - Classes INSERT/UPSERT binds 7 additional header fields
  - Hunter branch gains judge-score write block
  - Fresh-replace at CLASS level (not per-entry) — ensures entries
    whose data legitimately dropped to zero rows get stale rows
    purged. (Per-entry wipe was initially used and caused 379 stale
    zero-score rows to persist through the filter tightening.)

VALIDATION after final backfill (v_86ac9e00 live)
  classes with num_rounds populated:    16/16 hunter ✓
  classes with derby_type populated:    16/16 hunter ✓
  classes with is_championship=1:        8 (the "C" suffix classes)
  classes with sponsor text:             1 (most test classes unsponsored)
  classes with ihsa=1:                   0 (no IHSA test data)

  Judge score data:
    total judge rows:           86
    entries with judge data:    31 (of 221 hunter entries — rest are
                                    forced/EL/DNS with no score data)
    rows with real score > 0:   86 (all clean after zero-skip fix)
    derby HighOptions rows:     33
    derby HandyBonus rows:      15

  Spot-check class 1001 derby 2-judge entry 111 PEACHES:
    R1 J0: base=89, ho=4 ✓
    R1 J1: base=90, ho=4 ✓
    R2 J0: base=89, ho=4, handy=10 ✓
    R2 J1: base=83, ho=4, handy=5 ✓

  Spot-check class 1002 non-derby 7-judge test entry 117 RULE ON:
    R1 J0-J6: base_score 1-7 (test values) ✓
    R2 J0-J6: base_score 1-7 ✓
    R3: 0 rows (class is 2-round; R3 cols were all zero)  ✓

STILL DEFERRED (display / polish layer)
  - HUNTER_MODES labels (Bill auditing Ryegate settings)
  - Ribbon graphics placement using ribbon_count
  - Ch/Res graphics on is_championship=1 classes for places 1-2
  - Sponsor rendering on public page
  - Derby component breakdown in admin display
  - Per-judge breakdown rendering
  - Public results page (Phase 2e) with ELIM→EL collapse

DATA COLLECTION FOR HUNTERS: ✓ COMPLETE
  Every column known per CLS-FORMAT spec is now captured when
  present. Nothing more to extract from the .cls for hunters.
  Display work is separate concern.

═══════════════════════════════════════════════════════════════════════════
ARCHITECTURAL NOTE — VENDOR PLUGGABILITY (Bill, end of session)
═══════════════════════════════════════════════════════════════════════════

Bill asked: could the lens module be factored to support other scoring
software vendors (not just Ryegate)? Not a directive — exploration.

Answer: yes, architecture is ALREADY half-decomposed correctly.

Vendor-NEUTRAL (works regardless of source):
  - D1 schema (entries, entry_jumper_*, entry_hunter_*) is canonical
  - west-rules.js — placement rules are domain-level (sport rules,
    not Ryegate rules)
  - west-status.js — EL/RT/WD/DNS are USEF/FEI standard codes
  - Admin / results / stats pages read canonical schema

Vendor-SPECIFIC today (Ryegate-only, would need siblings to plug in):
  - west-cls-jumper.js / west-cls-hunter.js (column positions)
  - parseEntriesScoreJ / parseEntriesScoreH in worker (read CSV)
  - Engine watches Ryegate folder + Ryegate UDP

Future shape (hypothetical, not built):
  v3/js/vendors/ryegate/cls-{jumper,hunter}.js     ← move existing here
  v3/js/vendors/fei/xml-{jumper,hunter}-spec.js    ← future
  classes table gains source_vendor TEXT
  Engine config has vendor field

Three optional structural moves available anytime to keep the door
open without building generalization:
  1. Rename v3/js/west-cls-* → v3/js/vendors/ryegate/cls-*
  2. ALTER TABLE classes ADD COLUMN source_vendor TEXT DEFAULT 'ryegate'
  3. Add vendor field to engine config (default 'ryegate')

Each ~10 min, zero risk. Pays off when a second vendor materializes.
Until then, current single-vendor code is fine.

NOT premature-generalizing now: no plugin registries, no abstract
parser interfaces, no vendor-agnostic refactor of the existing
Ryegate parser. Build the second vendor's adapter when a second
vendor is real.

═══════════════════════════════════════════════════════════════════════════
NEXT UP (post-session-33)
═══════════════════════════════════════════════════════════════════════════

- Phase 2e PUBLIC RESULTS PAGE — thin view using shared modules.
  publicLabel() collapses ELIM family to generic "EL".
  First consumer of west-rules.js placement logic outside admin.
- Per-judge scores — new table entry_hunter_judge_scores,
  separate parser pass. Bill to audit Ryegate settings for
  HUNTER_MODES labels first.
- Derby component breakdown — extended hunter parser pass
  (classMode=2 specific), feeds a judge-aware rendering.
- Phase 3+ STATS — per-round shape ready for native SQL aggregation
  across both lenses. First consumer likely per-class clear-round %
  dashboard. No unpivot gymnastics needed in either direction.

═══════════════════════════════════════════════════════════════════════════
FILES CHANGED THIS SESSION
═══════════════════════════════════════════════════════════════════════════

NEW:
  v3/migrations/009_entry_jumper_per_round.sql
  session-notes/SESSION-NOTES-33.txt (this file)

MODIFIED:
  west-worker.js (per-round write path, pivot read path)
  docs/v3-planning/SESSION-32-JUMPER-STATUS-FINDINGS.md (§ schema
    pivot note + terminology updates)
